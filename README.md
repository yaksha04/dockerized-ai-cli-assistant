# dockerized-ai-cli-assistant
This assistant understands natural language commands and automates tasks like git operations and system monitoring, all packaged inside a Docker container for easy deployment.
